# Airflow
AIRFLOW_UID=501

# Pipeline database — matches the docker-compose.yaml defaults
PIPELINE_DB_HOST=localhost
PIPELINE_DB_PORT=5433
PIPELINE_DB_NAME=pipeline
PIPELINE_DB_USER=pipeline
PIPELINE_DB_PASSWORD=pipeline

# ND-GAIN CSV — set one of these
# ND_GAIN_CSV_URL=https://...
# ND_GAIN_CSV_PATH=/path/to/gain_index.csv
